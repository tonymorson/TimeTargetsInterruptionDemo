@testable import TimeTargets
import XCTest

final class TimeTargetsTests: XCTestCase {
  func testExample() {
    // This is an example of a functional test case.
    // Use XCTAssert and related functions to verify your tests produce the correct
    // results.
    XCTAssertEqual(TimeTargets().text, "Hello, World!")
  }
}
